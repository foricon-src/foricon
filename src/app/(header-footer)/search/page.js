import PageClient from "./page.client";

export const metadata = {
    title: 'Browse the library',
    description: 'Search for any icons that fit your need in our set of 1000+ icons available across 4 styles. Easily intergrate into your project through HTML codes, or as high-quality PNG, SVG or WebP images',
}
export default function Page() {
    return <PageClient/>
}